options(papersize="a4") 
options(editor="vi") 
options(tab.width = 2) 
options(width = 238)
options(graphics.record=TRUE) 

.First <- function(){
  cat("\nWelcome at", date(), "\n") 
}

.Last <- function(){ 
  cat("\nGoodbye at ", date(), "\n")
}
